# Paste ONLY the three lines below into Streamlit Cloud → Settings → Secrets.
# Rules: double quotes, KEY = "value", no ``` fences, no .env KEY=value format.

OPENAI_API = "nvapi-xxxxxxxx"
OPENAI_BASE_URL = "https://integrate.api.nvidia.com/v1"
OPENAI_MODEL = "meta/llama-3.1-70b-instruct"
